package com.tmorgner.autocake.example.api;

/**
 * Canonical example code. There is no way I could provide less, and I really tried.
 */
public interface HelloWorldService {
  String getGreeting(String name);
}
